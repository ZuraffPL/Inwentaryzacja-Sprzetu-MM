<UserControl x:Class="InwentaryzacjaSprzetu.Views.LocationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek i przyciski -->
        <Border Grid.Row="0" Background="#E8E8E8" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Zarządzanie Pawilonami" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Dodaj nową" Margin="20,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.AddLocationCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Edytuj" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.EditLocationCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Usuń" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.DeleteLocationCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Border>

        <!-- Lista lokalizacji -->
        <DataGrid Grid.Row="1" 
                  x:Name="LocationsDataGrid"
                  ItemsSource="{Binding DataContext.Locations, RelativeSource={RelativeSource AncestorType=Window}}"
                  SelectedItem="{Binding DataContext.SelectedLocation, RelativeSource={RelativeSource AncestorType=Window}}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  CellEditEnding="LocationsDataGrid_CellEditEnding"
                  PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            
            <!-- Menu kontekstowe — obsługiwane przez code-behind (wzorzec jak EquipmentView) -->
            <DataGrid.ContextMenu>
                <ContextMenu Opened="LocationContextMenu_Opened">
                    <MenuItem x:Name="MenuItemEdit" Header="Edytuj" Click="MenuItemEdit_Click">
                        <MenuItem.Icon>
                            <TextBlock Text="✏️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuItemDelete" Header="Usuń" Click="MenuItemDelete_Click">
                        <MenuItem.Icon>
                            <TextBlock Text="🗑️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod" Binding="{Binding Code}" Width="Auto"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding Address}" Width="Auto"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Kod pawilonu" Binding="{Binding PavilionCode}" Width="SizeToHeader"/>
                <DataGridTextColumn Header="Kraj" Binding="{Binding CountryCode}" Width="SizeToHeader"
                                    IsReadOnly="True" ElementStyle="{x:Null}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="ToolTip" Value="Kod kraju: PL, SK, CZ, RO, HU, HR"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Data utworzenia" Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy HH:mm}" Width="SizeToHeader" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Width="SizeToHeader" IsReadOnly="False">
                    <DataGridCheckBoxColumn.Header>
                        <TextBlock Text="Aktywna" ToolTip="Zaznaczono = zakładka pawilonu widoczna w widoku Sprzęt&#x0a;Odznaczono = zakładka ukryta"/>
                    </DataGridCheckBoxColumn.Header>
                    <DataGridCheckBoxColumn.Binding>
                        <Binding Path="IsActive" UpdateSourceTrigger="PropertyChanged"/>
                    </DataGridCheckBoxColumn.Binding>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>