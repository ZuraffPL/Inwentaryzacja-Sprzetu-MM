<Window x:Class="InwentaryzacjaSprzetu.Views.ConnectedEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ComponentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:helpers="clr-namespace:InwentaryzacjaSprzetu.Helpers"
        Title="Sprzęty podpięte do komputera"
        Width="950" Height="600"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <helpers:EnumToStringConverter x:Key="EnumToStringConverter"/>
        <helpers:CategoryToPastelBackgroundConverter x:Key="CategoryToPastelBackgroundConverter"/>

        <CollectionViewSource x:Key="EquipmentGrouped" Source="{Binding Items}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Category.Name"/>
            </CollectionViewSource.GroupDescriptions>
            <CollectionViewSource.SortDescriptions>
                <ComponentModel:SortDescription PropertyName="Category.SortOrder" Direction="Ascending"/>
                <ComponentModel:SortDescription PropertyName="InventoryNumber" Direction="Ascending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>

        <Style x:Key="RowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#5B9BD5"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek z informacją o komputerze -->
        <Border Grid.Row="0" Background="#E8E8E8" Padding="14,10">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="17" FontWeight="Bold" Foreground="#222"/>
                <TextBlock x:Name="SubtitleText" FontSize="12" Foreground="#666" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <!-- DataGrid z grupowaniem per kategoria -->
        <DataGrid Grid.Row="1"
                  x:Name="ItemsDataGrid"
                  ItemsSource="{Binding Source={StaticResource EquipmentGrouped}}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternationCount="2"
                  RowStyle="{StaticResource RowStyle}"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#5B9BD5"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
            </DataGrid.Resources>

            <!-- Grupowanie po kategorii -->
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Border Background="{Binding Name, Converter={StaticResource CategoryToPastelBackgroundConverter}}"
                                                CornerRadius="4" Margin="0,2" Padding="5">
                                            <Expander IsExpanded="True" Background="Transparent">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="#333"/>
                                                        <TextBlock Text="{Binding ItemCount, StringFormat=' ({0} szt.)'}" FontSize="12" Foreground="#666" Margin="5,0"/>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <ItemsPresenter Margin="12,4,0,4"/>
                                            </Expander>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr Inwentarzowy" Binding="{Binding InventoryNumber}" Width="140" MinWidth="100"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*" MinWidth="120"/>
                <DataGridTextColumn Header="Marka" Binding="{Binding Brand}" Width="90" MinWidth="70"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="100" MinWidth="70"/>
                <DataGridTextColumn Header="Nr Seryjny" Binding="{Binding SerialNumber}" Width="110" MinWidth="80"/>
                <DataGridTextColumn Header="Lokalizacja" Binding="{Binding Location.Name}" Width="100" MinWidth="80"/>
                <DataGridTextColumn Header="Gdzie?" Binding="{Binding LocationDetails}" Width="90" MinWidth="60"/>
                <DataGridTextColumn Header="Dział" Binding="{Binding Department.Name}" Width="90" MinWidth="60"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status, Converter={StaticResource EnumToStringConverter}}" Width="110" MinWidth="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Dolny pasek: licznik + zamknij -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="10,8"
                BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Zamknij" Padding="20,5"
                        IsCancel="True" Click="CloseButton_Click"/>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="#555" FontSize="12"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
