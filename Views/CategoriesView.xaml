<UserControl x:Class="InwentaryzacjaSprzetu.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:InwentaryzacjaSprzetu.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <helpers:CategoryToPastelBackgroundConverter x:Key="CategoryToPastelBackgroundConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek i przyciski -->
        <Border Grid.Row="0" Background="#E8E8E8" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Zarządzanie Kategoriami" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Dodaj nową" Margin="20,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.AddCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Edytuj" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Usuń" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Border>

        <!-- Lista kategorii -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding DataContext.Categories, RelativeSource={RelativeSource AncestorType=Window}}"
                  SelectedItem="{Binding DataContext.SelectedCategory, RelativeSource={RelativeSource AncestorType=Window}}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="Transparent"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            
            <!-- Style dla wierszy z kolorami kategorii -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{Binding Name, Converter={StaticResource CategoryToPastelBackgroundConverter}}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#4A90E2"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#D0E8FF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            
            <!-- Menu kontekstowe -->
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edytuj" 
                              Command="{Binding DataContext.EditCategoryWithParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                        <MenuItem.Icon>
                            <TextBlock Text="✏️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Usuń" 
                              Command="{Binding DataContext.DeleteCategoryWithParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                        <MenuItem.Icon>
                            <TextBlock Text="🗑️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod" Binding="{Binding Code}" Width="Auto"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Data utworzenia" Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy HH:mm}" Width="SizeToHeader" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Aktywna" Binding="{Binding IsActive}" Width="SizeToHeader"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>