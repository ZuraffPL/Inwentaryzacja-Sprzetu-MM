<UserControl x:Class="InwentaryzacjaSprzetu.Views.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek i przyciski -->
        <Border Grid.Row="0" Background="#E8E8E8" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Zarządzanie Działami" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Dodaj nowy" Margin="20,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.AddDepartmentCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Edytuj" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.EditDepartmentCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Usuń" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding DataContext.DeleteDepartmentCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Border>

        <!-- Lista działów -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding DataContext.Departments, RelativeSource={RelativeSource AncestorType=Window}}"
                  SelectedItem="{Binding DataContext.SelectedDepartment, RelativeSource={RelativeSource AncestorType=Window}}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            
            <!-- Menu kontekstowe -->
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edytuj" 
                              Command="{Binding DataContext.EditDepartmentWithParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                        <MenuItem.Icon>
                            <TextBlock Text="✏️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Usuń" 
                              Command="{Binding DataContext.DeleteDepartmentWithParameterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                        <MenuItem.Icon>
                            <TextBlock Text="🗑️" FontSize="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod" Binding="{Binding Code}" Width="Auto"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Data utworzenia" Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy HH:mm}" Width="SizeToHeader" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Aktywny" Binding="{Binding IsActive}" Width="SizeToHeader"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>